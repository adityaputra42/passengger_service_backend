// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"pasengger_service/ent/flight"
	"pasengger_service/ent/flightseat"
	"pasengger_service/ent/seats"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// FlightSeat is the model entity for the FlightSeat schema.
type FlightSeat struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// Price holds the value of the "price" field.
	Price float64 `json:"price,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FlightSeatQuery when eager-loading is set.
	Edges                  FlightSeatEdges `json:"edges"`
	flight_flight_seats    *uuid.UUID
	seats_flight_instances *uuid.UUID
	selectValues           sql.SelectValues
}

// FlightSeatEdges holds the relations/edges for other nodes in the graph.
type FlightSeatEdges struct {
	// Flight holds the value of the flight edge.
	Flight *Flight `json:"flight,omitempty"`
	// Seat holds the value of the seat edge.
	Seat *Seats `json:"seat,omitempty"`
	// Assigned holds the value of the assigned edge.
	Assigned []*BookingPassenger `json:"assigned,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// FlightOrErr returns the Flight value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FlightSeatEdges) FlightOrErr() (*Flight, error) {
	if e.Flight != nil {
		return e.Flight, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: flight.Label}
	}
	return nil, &NotLoadedError{edge: "flight"}
}

// SeatOrErr returns the Seat value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FlightSeatEdges) SeatOrErr() (*Seats, error) {
	if e.Seat != nil {
		return e.Seat, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: seats.Label}
	}
	return nil, &NotLoadedError{edge: "seat"}
}

// AssignedOrErr returns the Assigned value or an error if the edge
// was not loaded in eager-loading.
func (e FlightSeatEdges) AssignedOrErr() ([]*BookingPassenger, error) {
	if e.loadedTypes[2] {
		return e.Assigned, nil
	}
	return nil, &NotLoadedError{edge: "assigned"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FlightSeat) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case flightseat.FieldPrice:
			values[i] = new(sql.NullFloat64)
		case flightseat.FieldStatus:
			values[i] = new(sql.NullString)
		case flightseat.FieldID:
			values[i] = new(uuid.UUID)
		case flightseat.ForeignKeys[0]: // flight_flight_seats
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case flightseat.ForeignKeys[1]: // seats_flight_instances
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FlightSeat fields.
func (_m *FlightSeat) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case flightseat.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case flightseat.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case flightseat.FieldPrice:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price", values[i])
			} else if value.Valid {
				_m.Price = value.Float64
			}
		case flightseat.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field flight_flight_seats", values[i])
			} else if value.Valid {
				_m.flight_flight_seats = new(uuid.UUID)
				*_m.flight_flight_seats = *value.S.(*uuid.UUID)
			}
		case flightseat.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field seats_flight_instances", values[i])
			} else if value.Valid {
				_m.seats_flight_instances = new(uuid.UUID)
				*_m.seats_flight_instances = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the FlightSeat.
// This includes values selected through modifiers, order, etc.
func (_m *FlightSeat) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryFlight queries the "flight" edge of the FlightSeat entity.
func (_m *FlightSeat) QueryFlight() *FlightQuery {
	return NewFlightSeatClient(_m.config).QueryFlight(_m)
}

// QuerySeat queries the "seat" edge of the FlightSeat entity.
func (_m *FlightSeat) QuerySeat() *SeatsQuery {
	return NewFlightSeatClient(_m.config).QuerySeat(_m)
}

// QueryAssigned queries the "assigned" edge of the FlightSeat entity.
func (_m *FlightSeat) QueryAssigned() *BookingPassengerQuery {
	return NewFlightSeatClient(_m.config).QueryAssigned(_m)
}

// Update returns a builder for updating this FlightSeat.
// Note that you need to call FlightSeat.Unwrap() before calling this method if this FlightSeat
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *FlightSeat) Update() *FlightSeatUpdateOne {
	return NewFlightSeatClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the FlightSeat entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *FlightSeat) Unwrap() *FlightSeat {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: FlightSeat is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *FlightSeat) String() string {
	var builder strings.Builder
	builder.WriteString("FlightSeat(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("price=")
	builder.WriteString(fmt.Sprintf("%v", _m.Price))
	builder.WriteByte(')')
	return builder.String()
}

// FlightSeats is a parsable slice of FlightSeat.
type FlightSeats []*FlightSeat

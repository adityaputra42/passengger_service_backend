// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"pasengger_service/ent/aircraft"
	"pasengger_service/ent/flightseat"
	"pasengger_service/ent/predicate"
	"pasengger_service/ent/seats"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// SeatsQuery is the builder for querying Seats entities.
type SeatsQuery struct {
	config
	ctx                 *QueryContext
	order               []seats.OrderOption
	inters              []Interceptor
	predicates          []predicate.Seats
	withAircraft        *AircraftQuery
	withFlightInstances *FlightSeatQuery
	withFKs             bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SeatsQuery builder.
func (_q *SeatsQuery) Where(ps ...predicate.Seats) *SeatsQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SeatsQuery) Limit(limit int) *SeatsQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SeatsQuery) Offset(offset int) *SeatsQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SeatsQuery) Unique(unique bool) *SeatsQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SeatsQuery) Order(o ...seats.OrderOption) *SeatsQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAircraft chains the current query on the "aircraft" edge.
func (_q *SeatsQuery) QueryAircraft() *AircraftQuery {
	query := (&AircraftClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seats.Table, seats.FieldID, selector),
			sqlgraph.To(aircraft.Table, aircraft.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seats.AircraftTable, seats.AircraftColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFlightInstances chains the current query on the "flight_instances" edge.
func (_q *SeatsQuery) QueryFlightInstances() *FlightSeatQuery {
	query := (&FlightSeatClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seats.Table, seats.FieldID, selector),
			sqlgraph.To(flightseat.Table, flightseat.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, seats.FlightInstancesTable, seats.FlightInstancesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Seats entity from the query.
// Returns a *NotFoundError when no Seats was found.
func (_q *SeatsQuery) First(ctx context.Context) (*Seats, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{seats.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SeatsQuery) FirstX(ctx context.Context) *Seats {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Seats ID from the query.
// Returns a *NotFoundError when no Seats ID was found.
func (_q *SeatsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{seats.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SeatsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Seats entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Seats entity is found.
// Returns a *NotFoundError when no Seats entities are found.
func (_q *SeatsQuery) Only(ctx context.Context) (*Seats, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{seats.Label}
	default:
		return nil, &NotSingularError{seats.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SeatsQuery) OnlyX(ctx context.Context) *Seats {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Seats ID in the query.
// Returns a *NotSingularError when more than one Seats ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SeatsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{seats.Label}
	default:
		err = &NotSingularError{seats.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SeatsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SeatsSlice.
func (_q *SeatsQuery) All(ctx context.Context) ([]*Seats, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Seats, *SeatsQuery]()
	return withInterceptors[[]*Seats](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SeatsQuery) AllX(ctx context.Context) []*Seats {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Seats IDs.
func (_q *SeatsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(seats.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SeatsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SeatsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SeatsQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SeatsQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SeatsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SeatsQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SeatsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SeatsQuery) Clone() *SeatsQuery {
	if _q == nil {
		return nil
	}
	return &SeatsQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]seats.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.Seats{}, _q.predicates...),
		withAircraft:        _q.withAircraft.Clone(),
		withFlightInstances: _q.withFlightInstances.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithAircraft tells the query-builder to eager-load the nodes that are connected to
// the "aircraft" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeatsQuery) WithAircraft(opts ...func(*AircraftQuery)) *SeatsQuery {
	query := (&AircraftClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAircraft = query
	return _q
}

// WithFlightInstances tells the query-builder to eager-load the nodes that are connected to
// the "flight_instances" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeatsQuery) WithFlightInstances(opts ...func(*FlightSeatQuery)) *SeatsQuery {
	query := (&FlightSeatClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFlightInstances = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SeatNumber string `json:"seat_number,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Seats.Query().
//		GroupBy(seats.FieldSeatNumber).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SeatsQuery) GroupBy(field string, fields ...string) *SeatsGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SeatsGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = seats.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SeatNumber string `json:"seat_number,omitempty"`
//	}
//
//	client.Seats.Query().
//		Select(seats.FieldSeatNumber).
//		Scan(ctx, &v)
func (_q *SeatsQuery) Select(fields ...string) *SeatsSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SeatsSelect{SeatsQuery: _q}
	sbuild.label = seats.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SeatsSelect configured with the given aggregations.
func (_q *SeatsQuery) Aggregate(fns ...AggregateFunc) *SeatsSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SeatsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !seats.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SeatsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Seats, error) {
	var (
		nodes       = []*Seats{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withAircraft != nil,
			_q.withFlightInstances != nil,
		}
	)
	if _q.withAircraft != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, seats.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Seats).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Seats{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAircraft; query != nil {
		if err := _q.loadAircraft(ctx, query, nodes, nil,
			func(n *Seats, e *Aircraft) { n.Edges.Aircraft = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withFlightInstances; query != nil {
		if err := _q.loadFlightInstances(ctx, query, nodes,
			func(n *Seats) { n.Edges.FlightInstances = []*FlightSeat{} },
			func(n *Seats, e *FlightSeat) { n.Edges.FlightInstances = append(n.Edges.FlightInstances, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SeatsQuery) loadAircraft(ctx context.Context, query *AircraftQuery, nodes []*Seats, init func(*Seats), assign func(*Seats, *Aircraft)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Seats)
	for i := range nodes {
		if nodes[i].aircraft_seats == nil {
			continue
		}
		fk := *nodes[i].aircraft_seats
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(aircraft.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "aircraft_seats" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeatsQuery) loadFlightInstances(ctx context.Context, query *FlightSeatQuery, nodes []*Seats, init func(*Seats), assign func(*Seats, *FlightSeat)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Seats)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.FlightSeat(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(seats.FlightInstancesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.seats_flight_instances
		if fk == nil {
			return fmt.Errorf(`foreign-key "seats_flight_instances" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "seats_flight_instances" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *SeatsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SeatsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(seats.Table, seats.Columns, sqlgraph.NewFieldSpec(seats.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, seats.FieldID)
		for i := range fields {
			if fields[i] != seats.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SeatsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(seats.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = seats.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SeatsGroupBy is the group-by builder for Seats entities.
type SeatsGroupBy struct {
	selector
	build *SeatsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SeatsGroupBy) Aggregate(fns ...AggregateFunc) *SeatsGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SeatsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeatsQuery, *SeatsGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SeatsGroupBy) sqlScan(ctx context.Context, root *SeatsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SeatsSelect is the builder for selecting fields of Seats entities.
type SeatsSelect struct {
	*SeatsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SeatsSelect) Aggregate(fns ...AggregateFunc) *SeatsSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SeatsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeatsQuery, *SeatsSelect](ctx, _s.SeatsQuery, _s, _s.inters, v)
}

func (_s *SeatsSelect) sqlScan(ctx context.Context, root *SeatsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
